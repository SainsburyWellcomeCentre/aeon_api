# Python environment yaml setup file
# run e.g. w/mamba: `mamba env create -qf env.yml`

name: aeon
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9, <3.10
  - pip
  - dotmap
  - fastparquet
  - graphviz
  - matplotlib
  - numpy>=1.21.0, <2
  - pandas>=1.3, <2
  - scikit-learn
  - scipy
  - seaborn
  - bandit # ....................... (dev)
  - black # ........................ (dev)
  - isort # ........................ (dev)
  - pre-commit # ................... (dev)
  - pytest # ....................... (dev)
  - pytest-cov # ................... (dev)
  - pip:
    - opencv-python